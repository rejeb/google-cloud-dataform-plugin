config {
    type: "table",
    schema: `${dataform.projectConfig.vars.env}_${dataform.projectConfig.vars.aggregated}`,
    description: "aggregated ma_table",
    tags: ["aggregated"]
}

js {

}

    SELECT NUMERO_CONTRAT
    FROM
${ref("ma_table")}
GROUP BY
    SOURCE, NUMERO_CONTRAT
HAVING
    COUNT(*) > 1